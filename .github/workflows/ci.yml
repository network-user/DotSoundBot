# Full Bot lint/test CI is intentionally not enabled for the public
# source-available showcase yet.
#
# Why:
# - The repository depends on the private policy package for complete
#   installation and Docker builds.
# - The historical Ruff/Mypy backlog is not advertised as green.
# - Active public guardrails live in `policy-guardrails.yml`.
