repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-added-large-files
        args: ['--maxkb=1024']
      - id: detect-private-key

# Ruff/Black hooks are intentionally left disabled for now: the public
# showcase carries a known lint/format backlog (see .github/workflows/ci.yml),
# so auto-fixing on every commit would be disruptive. Re-enable once the
# backlog is cleared.
#  - repo: https://github.com/astral-sh/ruff-pre-commit
#    rev: v0.5.7
#    hooks:
#      - id: ruff
#        args: [--fix, --exit-non-zero-on-fix]
#        files: ^bot/
#      - id: ruff-format
#        files: ^bot/
#
#  - repo: https://github.com/psf/black
#    rev: 24.10.0
#    hooks:
#      - id: black
#        args: [--line-length=79]
#        files: ^bot/
